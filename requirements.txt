# Pinned <2.0: mcp 2.x removed mcp.server.fastmcp (FastMCP moved out of the
# SDK). An unpinned rebuild pulled 2.0.0 on 2026-07-29 and every MCP server
# on CT 207 failed to start -- the agent's whole tool layer went dark.
mcp>=1.2.0,<2.0
httpx>=0.27.0
pydantic>=2.6.0
python-dotenv>=1.0.0
asyncssh>=2.18.0
